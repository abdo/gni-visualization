const INITIAL_STATE = {
  area: 'World',
};

const chartConfigReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default chartConfigReducer;
